// Bundle Proxy — serves content-addressed room bundles with token verification and disk LRU cache.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Println("bundle-proxy starting...")
	os.Exit(0)
}
